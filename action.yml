# action.yml
name: 'ssync'
author: 'Christopher Wallace, Mark Keaton'
description: 'Sync a NetSuite environment with a local repository.'
branding:
  icon: 'download-cloud'
  color: 'red'
runs:
  using: 'docker'
  image: 'Dockerfile'
